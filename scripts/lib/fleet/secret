#!/usr/bin/env bash
# fw-summary: manage encrypted MinIO creds in fleet_config (gen-key/set/show)
source "$(dirname "${BASH_SOURCE[0]}")/../_helpers/_bootstrap.sh"
exec "$FW_LIB/fleet/config" secret "$@"
