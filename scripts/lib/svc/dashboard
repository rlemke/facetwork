#!/usr/bin/env bash
# Start the Facetwork web dashboard.
#
# Usage:
#   fw svc dashboard                              # start on port 8080
#   fw svc dashboard --port 9000                  # custom port
#   fw svc dashboard --config /path/to/config.json
#   fw svc dashboard --reload                     # auto-reload for dev
#   fw svc dashboard --help                       # show all options

source "$(dirname "${BASH_SOURCE[0]}")/../_helpers/_bootstrap.sh"
set -euo pipefail

REPO_ROOT="$FW_ROOT"

PYTHON="${REPO_ROOT}/.venv/bin/python3"
[[ -x "$PYTHON" ]] || PYTHON=python3

exec env PYTHONPATH="$REPO_ROOT" "$PYTHON" -m facetwork.dashboard "$@"
